{% render 'partials/head.liquid', title: 'Squad page' %}


<section>
    <h2> {{ person.name }}</h2>
    <img class="border-img" src="{{ person.avatar }}" style="border: 15px solid {{ person.fav_color }};">
    <a href="/kleur">Back</a>
</section>


{% render 'partials/foot.liquid', squads: squads %}